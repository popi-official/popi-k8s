apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: popi
spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}